services:
  captureindia:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      APP_URL: http://localhost:3000
      DEMO_MODE: "true"
      PAYMENT_MODE: demo
    volumes:
      - captureindia-data:/app/data
    restart: unless-stopped
volumes:
  captureindia-data:
